import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TaskCard = () => {
  return (
    <View style={{ backgroundColor: "#fff", padding: 10, borderRadius: 10, shadowColor: "#000", shadowOpacity: 0.1, marginBottom: 10 }}>
      <Text>TaskCard</Text>
    </View>
  )
}

export default TaskCard

const styles = StyleSheet.create({})